# Local environment overrides. Copy to .env.local and fill in values.
# ANTHROPIC_API_KEY is managed via Electron safeStorage (not this file).
# MONGODB_URI=mongodb://127.0.0.1:27017/agent-desk
# AGENT_SERVER_PORT=3001
